function itinDateReducer (state={}, action) {

    switch(action.type) {
        case 'ADD_ITIN_DATE':
            return state
        default:
            return state
    }

}

export default itinDateReducer